# ── Backend ──────────────────────────────────────────────────────────────────
fastapi==0.129.0
uvicorn==0.40.0
starlette==0.52.1      # Direktimport in api/middleware.py
slowapi==0.1.9
pydantic==2.12.5

# ── Frontend ─────────────────────────────────────────────────────────────────
shiny==1.5.1
faicons==0.2.2
requests==2.32.5

# ── Raspberry Pi (nur auf dem Pi, auf Dev-Maschine weglassen) ────────────────
RPi.GPIO

psutil==6.1.1          # Disk-, RAM-, Uptime- und Netzwerk-Metriken

# ── Zeitzonen (für zoneinfo auf Systemen ohne IANA-DB, z.B. Windows) ─────────
tzdata==2025.3

# ── Tests ────────────────────────────────────────────────────────────────────
pytest==9.0.2
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-xdist==3.5.0
httpx==0.25.2           # Starlette TestClient
